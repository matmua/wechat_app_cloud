# 特色工具语义纠偏与设计清单

## 1. 本次结论

本次优先纠正四个语义偏差最大的工具：`树洞袜子`、`打发时间`、`对方天气`、`经期记录`。它们已经不再共用统一占位模板，而是分别改成隐藏悄悄话抽屉、街机小游戏中心、远方天气卡、私密关心日历。

本次没有修改 `login/couple` 云函数，也没有修改小程序名称“爱木长诗”。涉及情侣共享或敏感数据的页面都先读取当前绑定状态，未绑定时不写入数据；写入时包含 `coupleId`，私密内容包含 `ownerOpenid`。

## 2. 工具语义纠偏表

| 工具 | 正确用途 | 当前问题 | 本次改造 | 页面特色 | 数据集合 | 是否完成 | 后续测试 |
| -- | ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| 树洞袜子 | 暂时不想马上告诉对方的悄悄话，可隐藏或定时公开 | 之前被做成抽问题/聊天题 | 改成袜子抽屉：文本、图片、视频、音频；公开/隐藏/定时公开；作者可见全部，对方只看已公开或已到时间内容 | 小袜子、抽屉、掉出来的悄悄话 | `sock_secrets` | 前端和数据结构已完成 | 测文本/图片/视频/音频、三种可见性、到期展示 |
| 打发时间 | 无聊时玩简单小游戏 | 之前是挑战卡，不是小游戏中心 | 改成街机厅/卡带墙，已实现最小可玩的贪吃蛇；扫雷、打飞机、俄罗斯方块先占位 | 街机屏幕、方向键、游戏卡带 | 无 | 贪吃蛇已完成，其他游戏占位 | 测开始、方向、吃食物、撞墙/撞自己、暂停/重置 |
| 对方天气 | 显示对方所在地天气 | 之前是自己的天气/普通天气占位 | 改成远方天气卡：读取对方主动同步的城市/经纬度；自己可手动填写或主动选择位置同步 | 蓝色远方天气卡、等待同步状态、关心提示 | `partner_locations` | 页面结构和位置同步已完成，真实天气 API 未接入 | 测未绑定、未同步、自己同步、对方同步后读取 |
| 经期记录 | 生理期日历、预测、隐私关心提醒 | 之前是爱情故事占位，路由和入口错配 | 改成月历：记录开始/结束、预测下次、隐私模式：仅自己/关心模式/共享细节 | 月历格子、经期/预测/关心色块、隐私提示 | `period_records` | 前端和数据结构已完成 | 测三种隐私、预测、月历切换、对方关心提示 |
| 美食猜猜 | 随机决定今天吃什么 | 已改成抽卡工具 | 本次未继续改 | 抽卡/候选菜单/历史 | 本地存储 | 已有特色 | 测新增、删除、抽卡 |
| 心愿清单 | 愿望卡片、完成打勾、甜蜜标签 | 已有云数据功能 | 本次未改 | 卡片和完成状态 | 现有心愿集合 | 已有核心功能 | 后续回归云读写 |
| 相册 | 照片墙、回忆卡片 | 已有云存储功能 | 本次未改 | 照片墙、抽卡 | 现有相册集合 | 已有核心功能 | 后续回归上传/预览/删除 |
| 约会记录 | 时间轴、地点、心情 | 已有云数据功能 | 本次未改 | 时间轴、日历、海报 | 现有约会集合 | 已有核心功能 | 后续回归地图/图片/海报 |
| 纪念日 | 倒计时、重要日期 | 已有核心页面，另有占位入口 | 本次未改 | 倒计时卡片 | 现有纪念日集合 | 已有核心功能 | 后续合并重复入口 |
| 目标计划 | 两人目标和进度条 | 已改成本地 `coupleId` 隔离进度页 | 本次未改 | 圆形进度条 | 本地按 `coupleId` 存储 | 最小可用 | 后续决定是否入库 |
| 心动瞬间/纸条/金库/任务日志 | 碎片、信封、积分、打卡 | 已在 `heartbeat` 页面内 | 本次未改 | 盲盒、纸条、积分、任务 | 现有心动相关集合 | 已有核心功能 | 后续按子模块真机测试 |

## 3. 树洞袜子如何隐藏和定时公开

- 新集合：`sock_secrets`。
- 写入字段：`coupleId`、`ownerOpenid`、`contentType`、`text`、`fileIDs`、`visibility`、`revealAt`、`createdAt`、`updatedAt`。
- `visibility = public`：保存后对方可见。
- `visibility = hidden`：只查询和展示给作者自己。
- `visibility = scheduled`：作者自己随时可见；对方只查询 `revealAt <= 当前时间` 的内容。
- 图片、视频、音频会先上传到云存储，数据库只保存 `fileID`，不会保存本地临时路径。

生产环境建议继续补云函数或数据库权限规则，确保非作者不能绕过前端查询隐藏内容。

## 4. 打发时间现在有哪些小游戏

- 已实现：`贪吃蛇`，使用小程序基础视图格子和按钮控制，不依赖第三方游戏引擎。
- 占位卡带：`扫雷`、`打飞机`、`俄罗斯方块`。
- 后续可以逐个把占位卡带替换成独立小游戏，不需要删除入口。

## 5. 天气如何显示对方天气

- 新集合：`partner_locations`。
- 当前用户只能主动同步自己的城市/经纬度/更新时间。
- 页面读取绑定关系中的对方 `openid`，查询对方在当前 `coupleId` 下同步的位置。
- 对方未同步时显示“等待对方同步位置”。
- 当前天气数据是页面内模拟结果，用于验证结构和状态。
- 真实天气接口待后续新增 `weather` 云函数，由云函数保存 API key 并请求天气服务，前端不写 key。

## 6. 经期记录如何保护隐私

- 新集合：`period_records`。
- 每条记录包含 `coupleId`、`ownerOpenid`、`periodStart`、`periodEnd`、`cycleLength`、`periodLength`、`notes`、`visibility`。
- `private`：仅本人查询。
- `careOnly`：对方只看到“需要多关心”的提示，不展示日期和备注。
- `shared`：对方可看到共享日期和备注。
- 页面顶部有隐私提示，默认保存模式是 `careOnly`，避免默认暴露过多细节。

生产环境建议继续补云函数或数据库字段级权限，因为生理期属于敏感隐私数据。

## 7. 后续需要接云函数或真实 API

- `weather` 云函数：用 `coupleId/targetOpenid` 获取对方位置，在云函数里请求真实天气服务，API key 只放云端。
- `sock_secrets` 云函数：服务端按 `ownerOpenid/visibility/revealAt` 返回可见内容，避免前端权限过宽。
- `period_records` 云函数：服务端按隐私模式返回摘要或详情，避免 `careOnly` 记录泄露细节。

## 8. 微信开发者工具测试重点

1. 未绑定状态：树洞袜子、对方天气、经期记录都不能写入数据。
2. 树洞袜子：分别保存文字、图片、视频、音频；测试公开、隐藏、定时公开；确认数据库只保存 `fileID`。
3. 打发时间：贪吃蛇开始、方向键、吃食物、撞墙、暂停、重置。
4. 对方天气：自己手动填写城市并同步；另一个账号同步后，当前账号能看到对方城市和天气卡。
5. 经期记录：保存 private/careOnly/shared 三种记录；确认对方只看到允许展示的信息。
